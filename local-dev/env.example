# =====================================================
# TMC 本地开发环境变量示例
# =====================================================
# 复制此文件为 .env 并填写您的配置

# ===== 代理设置 =====
# 本地代理地址（格式：IP:端口）
LOCAL_PROXY_HOST=192.168.31.6:7890

# ===== Telegram 配置（完全可选）=====
# 【推荐】通过 Web 界面为每个客户端单独配置，无需在此填写
# 
# 以下配置仅作为默认值（fallback），当客户端没有单独配置时使用
# 大多数情况下，这些可以留空，所有配置都在 Web 界面完成
#
# 使用场景：
#   - 推荐方案：留空，所有客户端通过 Web 界面单独配置
#   - 可选方案：填写默认值，作为多个客户端的共享配置
#
# 如果需要填写，请取消注释以下配置：

# 从 https://my.telegram.org 获取
# API_ID=your_api_id
# API_HASH=your_api_hash

# Bot Token（从 @BotFather 获取）
# BOT_TOKEN=your_bot_token

# 管理员手机号（国际格式，如：+8613800138000）
# PHONE_NUMBER=+86138xxxxxxxx

# 管理员用户ID（多个用逗号分隔）
# ADMIN_USER_IDS=123456789,987654321

# ===== 应用配置 =====
# JWT 密钥（可选 - 用于 Web 管理界面的用户登录）
# 如果不设置，系统会自动生成随机密钥（重启后需要重新登录）
# 生产环境建议设置固定值以保持用户登录状态
# JWT_SECRET=your-super-secret-jwt-key-change-me

# 数据库地址（默认使用SQLite）
DATABASE_URL=sqlite:///app/data/app.db

# 日志级别（DEBUG, INFO, WARNING, ERROR）
LOG_LEVEL=INFO

# ===== 代理配置（运行时）=====
HTTP_PROXY=http://192.168.31.6:7890
HTTPS_PROXY=http://192.168.31.6:7890
NO_PROXY=localhost,127.0.0.1

