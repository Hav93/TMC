# =====================================================
# TMC 本地开发环境变量示例
# =====================================================
# 复制此文件为 .env 并填写您的配置

# ===== 代理设置 =====
# 本地代理地址（格式：IP:端口）
LOCAL_PROXY_HOST=192.168.31.6:7890

# ===== Telegram 配置（全局配置 - 可选）=====
# 注意：这些是全局配置，作为 fallback 使用
# 你可以通过网页端（客户端管理页面）为每个客户端单独配置 API_ID/API_HASH
# 
# 配置优先级：
#   1. 客户端配置（网页端添加时设置）- 优先使用
#   2. 全局配置（这里设置）- fallback
#
# 使用场景：
#   - 方案A：只在网页端设置客户端配置，这里留空或不填
#   - 方案B：在这里设置全局配置，所有客户端共享（不推荐多客户端场景）
#   - 方案C（推荐）：这里设置默认值，网页端可覆盖

# 从 https://my.telegram.org 获取（可选）
API_ID=your_api_id
API_HASH=your_api_hash

# Bot Token（从 @BotFather 获取，可选）
BOT_TOKEN=your_bot_token

# 管理员手机号（国际格式，如：+8613800138000，可选）
PHONE_NUMBER=+86138xxxxxxxx

# 管理员用户ID（多个用逗号分隔，可选）
ADMIN_USER_IDS=123456789,987654321

# ===== 应用配置 =====
# JWT 密钥（可选 - 用于 Web 管理界面的用户登录）
# 如果不设置，系统会自动生成随机密钥（重启后需要重新登录）
# 生产环境建议设置固定值以保持用户登录状态
# JWT_SECRET=your-super-secret-jwt-key-change-me

# 数据库地址（默认使用SQLite）
DATABASE_URL=sqlite:///app/data/app.db

# 日志级别（DEBUG, INFO, WARNING, ERROR）
LOG_LEVEL=INFO

# ===== 代理配置（运行时）=====
HTTP_PROXY=http://192.168.31.6:7890
HTTPS_PROXY=http://192.168.31.6:7890
NO_PROXY=localhost,127.0.0.1

