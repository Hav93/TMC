Alembic 数据库迁移工具

这个目录包含了数据库迁移脚本。

## 常用命令

### 生成迁移
```bash
# 自动检测模型变化并生成迁移
alembic revision --autogenerate -m "描述信息"

# 手动创建空白迁移
alembic revision -m "描述信息"
```

### 应用迁移
```bash
# 升级到最新版本
alembic upgrade head

# 升级指定步数
alembic upgrade +1

# 升级到指定版本
alembic upgrade <revision_id>
```

### 回滚迁移
```bash
# 降级一个版本
alembic downgrade -1

# 降级到指定版本
alembic downgrade <revision_id>

# 降级到初始状态
alembic downgrade base
```

### 查看历史
```bash
# 查看当前版本
alembic current

# 查看迁移历史
alembic history

# 查看详细历史
alembic history --verbose
```

## 更多信息

查看 ../DATABASE_MIGRATION_GUIDE.md 获取完整指南。

